export type Notice = {
  icon: string
  before: string
  highlight: string
  after: string
}

export type HeaderAction = {
  icon: string
  label: string
  size: number
}

export type Category = {
  name: string
  icon?: string
}
